square = lambda x:x*x # Take value as x and return value x*x as a output.

'''
def sum(x,y):
return x+y

In that type function we always try to write lambda function for better code.
'''

print(square(4))