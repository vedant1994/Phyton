name = "vedant" # Strings are immutable

# name[0] = "R" # We cant change string in memory

a = len(name) # Length of string
print(a)

print(name.upper(), name)
print(name.lower())
print(name.capitalize())
print(name.title())